# 第八周：深度研究助手

需求见 REQUIREMENTS.md。实现使用 Python 标准库后端和原生 HTML/CSS/JS，方便 Windows 本地运行。
运行：python app.py；测试：python -m unittest discover -s tests -v。
研究流程必须包含规划、检索、摘要抽取、补检判断和综合报告；保存每一步与来源。
演示模式必须明确标注，不可宣称实时联网。真实模式使用环境变量，不输出任何密钥。
前端用 textContent 或 HTML 转义显示用户输入、网页摘要与模型输出，不能直接执行模型 HTML。
修改限当前项目。证据只记录真实工具结果，不可伪造调用或成功信息。
